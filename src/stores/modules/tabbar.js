
const state = {
  comprehensiveQualityValue: 'homeBox'
}

const getters = {
}

const mutations = {
  changeValue (state, newVal) {
    state.comprehensiveQualityValue = newVal
  }
}

const actions = {}

export default {
  state,
  getters,
  mutations,
  actions
}
